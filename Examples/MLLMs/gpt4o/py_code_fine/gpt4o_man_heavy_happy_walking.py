
import matplotlib.pyplot as plt
import matplotlib.animation as animation
import numpy as np

# Define joint coordinates for "forward rolling" movement (simplified for visualization)
num_frames = 100  # Total frames in the animation
points_structure = 15  # Number of point-lights

# Function to generate "forward rolling" motion data
def generate_rolling_motion(num_frames, radius=1, speed=0.1):
    points = []
    
    # Simulating 15 joints rolling in a circular fashion
    angle_deltas = np.linspace(0, 2 * np.pi, points_structure, endpoint=False)
    
    # Calculating frames over time for each point-light
    for frame_ind in range(num_frames):
        frame_points = []
        current_angle = frame_ind* racing blocks>esting loops[pt -->
    
